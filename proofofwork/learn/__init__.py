"""The self-improving loop (plan §4): mine missed cheats -> propose a rule -> gate it hard
-> promote. ADD-ONLY — it can only ever grow detection power, never disable a check or shrink
coverage. Ground truth is the frozen, human-labeled corpus, never the LLM.
"""
